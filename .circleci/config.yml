version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:20.10
    steps:
      - checkout
      - run: node -v
      - run: npm -v

workflows:
  version: 2
  build_workflow:
    jobs:
      - build